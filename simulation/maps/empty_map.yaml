image: empty_map.pgm  # 仅占位，无需实际创建该.pgm文件
resolution: 0.05      # 地图分辨率（0.05米/像素，仿真场景无实际影响）
origin: [0.0, 0.0, 0.0]  # 地图原点与Gazebo世界原点对齐（关键！避免坐标偏移）
negate: 0              # 不反转地图黑白（默认值）
occupied_thresh: 0.65  # 像素值≥0.65判定为“障碍物”
free_thresh: 0.196     # 像素值≤0.196判定为“可通行区域”

