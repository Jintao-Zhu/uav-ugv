<?xml version="1.0"?>
<model>
  <name>red_box</name>  <!-- 模型名称（与 SDF 中 <model name> 一致） -->
  <version>1.0</version>
  <sdf version="1.6">model.sdf</sdf>  <!-- 指向 SDF 文件（文件名必须是 model.sdf） -->
  <author>
    <name>suda</name>  <!-- 你的名字（可选） -->
  </author>
  <description>
    A red cube (20cm) for drone navigation target.  <!-- 模型描述（可选） -->
  </description>
</model>
